<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>


	<!-- 指定Web应用的默认编码，相当于调用request的setCharacterEncoding方法 -->
	<constant name="struts.i18n.encoding" value="UTF-8" />
	<!-- 设置浏览器是否缓存静态内容，默认值为true(生产环境下使用)，开发阶段最好关闭 -->
	<constant name="struts.serve.static.browserCache" value="false" />
	<!-- 当Struts2的配置文件修改后，系统是否自动重新加载配置文件，默认值为false(生产环境下使用)，开发阶段最好打开 -->
	<constant name="struts.configuration.xml.reload" value="true" />
	<!-- 开发模式下使用，这样可以打印出更详细的日志信息 -->
	<constant name="struts.devMode" value="true" />
	<!-- 默认的视图主题 -->
	<constant name="struts.ui.theme" value="simple" />
	<!-- 把Action对象交给Spring创建和管理 -->
	<constant name="struts.objectFactory" value="spring" />
	<!-- Struts2处理的请求后缀,默认值是action -->
	<constant name="struts.action.extension" value="action" />
	<!-- 默认所有的结果页面存储在WEB-INF/content下 通过设置 struts.convention.result.path指定路径 -->
	<constant name="struts.convention.result.path" value="/WEB-INF/pages" />
	<!-- 包含子模块action -->
	<include file="struts/model/config/struts-config/struts-*.xml"></include>
	<package name="pts" extends="struts-default">
		<!-- 定义拦截器 <interceptors> <interceptor name="sessionout" class="com.tps.interceptor.SessionFilterInterceptor"> 
			</interceptor> 拦截器栈 <interceptor-stack name="tpsStack"> <interceptor-ref 
			name="defaultStack" /> <interceptor-ref name="sessionout"/> </interceptor-stack> 
			</interceptors> <global-results> <result name="tologin">index.jsp</result> 
			</global-results> <action name="user" method="login" class="userAction"> 
			<result name="success">/pages/result.jsp</result> <interceptor-ref name="tpsStack"></interceptor-ref> 
			</action> <action name="authCode" class="com.tps.common.utils.AuthCodeAction" 
			method="execute"> </action> -->
		<global-results>
			<result name="exception">/exception.jsp</result>
			<result name="commonPage">/commonPage.jsp</result>
		</global-results>

		<global-exception-mappings>
			<exception-mapping result="exception" exception="java.lang.Exception"></exception-mapping>
		</global-exception-mappings>
	</package>

</struts>

